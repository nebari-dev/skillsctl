apiVersion: v1
kind: Service
metadata:
  name: {{ include "skillsctl.fullname" . }}
  labels:
    {{- include "skillsctl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "skillsctl.selectorLabels" . | nindent 4 }}
